# 🌳 Магический лес в Roblox Studio

---

## 🧰 Что понадобится

- **Roblox Studio**
- **Пустая карта** (Baseplate)
- **3 модели объектов**:
  - `Tree` (дерево)
  - `Rock` (камень)
  - `Bush` (куст)

> Все объекты будут храниться в `ReplicatedStorage`.

---

## 🌲 Как создать дерево (`Tree`) с помощью Union

### Шаг 1. Создаём ствол
1. Нажми **Part → Block**
2. Сделай его высоким и тонким
3. Цвет — коричневый

### Шаг 2. Создаём крону
1. Нажми **Part → Sphere**
2. Увеличь шар
3. Цвет — зелёный
4. Поставь шар наверх ствола

### Шаг 3. Объединяем (Union)
1. Зажми **Shift** и выдели ствол + крону
2. Перейди во вкладку **Model**
3. Нажми **Union**

✨ Теперь это одно дерево!

### Шаг 4. Готовим модель
1. Переименуй `Union` в **Tree**
2. Перетащи `Tree` в **ReplicatedStorage**

---

## 🪨 Создание `Rock` и `Bush`

- **Rock** — любой серый `Part` или `Union`
- **Bush** — зелёный шар или несколько шаров

> ❗ Обязательно:
> - Названия: `Rock`, `Bush`
> - Хранятся в **ReplicatedStorage**

---

## 📜 Скрипт магического появления объектов

### Куда вставлять скрипт
1. Открой **ServerScriptService**
2. Нажми **+ Script**
3. Вставь код ниже

```lua
-- Получаем хранилище, где лежат заготовленные модели
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- Ищем в ReplicatedStorage объекты по имени
local Tree = ReplicatedStorage:FindFirstChild("Tree")
local Rock = ReplicatedStorage:FindFirstChild("Rock")
local Bush = ReplicatedStorage:FindFirstChild("Bush")

-- Размер зоны, где будут появляться объекты
-- Чем больше число — тем дальше они могут появиться
local spawnAreaSize = 100

-- Сколько объектов появится на карте
local numberOfObjects = 20

-- Функция, которая создаёт объекты
function spawnObjects()
	-- Цикл: повторяем numberOfObjects раз
	for i = 1, numberOfObjects do
		-- Создаём список объектов
		local objects = {Tree, Rock, Bush}

		-- Выбираем случайный объект из списка
		local object = objects[math.random(1, #objects)]

		-- Делаем копию выбранного объекта
		local clone = object:Clone()

		-- Случайная позиция по X и Z
		local x = math.random(-spawnAreaSize, spawnAreaSize)
		local z = math.random(-spawnAreaSize, spawnAreaSize)

		-- Ставим объект на карту
		-- Y = 1 — чтобы он не провалился под землю
		clone.Position = Vector3.new(x, 1, z)

		-- Добавляем объект в мир игры
		clone.Parent = workspace

		-- Небольшая пауза, чтобы объекты появлялись красиво
		wait(0.1)
	end
end

-- Запускаем функцию
spawnObjects()