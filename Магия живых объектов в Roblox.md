
## «Магия живых объектов в Roblox»  

## 🔹 ЗАДАНИЕ 1. ПАТРУЛЬНЫЙ КУБ  

### 🧱 Моделирование  
1. Создай **Part** в Workspace  
2. Назови его `GuardCube`  
3. Увеличь размер  
4. Включи `Anchored = true`  

### 💻 Код (`Script` в `GuardCube`)  
```lua
-- Берём сам куб
local cube = script.Parent

-- Запоминаем стартовую позицию
local startPos = cube.Position

-- Максимальное расстояние патруля
local distance = 20

-- Скорость движения
local speed = 0.1

-- Направление (1 = вперёд, -1 = назад)
local direction = 1

-- Бесконечный цикл
while true do
	-- Двигаем куб
	cube.Position = cube.Position + Vector3.new(speed * direction, 0, 0)

	-- Проверяем, не ушёл ли куб слишком далеко
	if (cube.Position - startPos).Magnitude >= distance then
		-- Меняем направление движения
		direction = direction * -1
	end

	-- Небольшая пауза
	wait(0.03)
end

```
🧠 Краткое объяснение
Куб запоминает стартовую точку, двигается в одну сторону и, когда доходит до края (проверка через .Magnitude), разворачивается и идёт обратно.



## 🔹 ЗАДАНИЕ 2. МАГИЧЕСКИЙ СПАВНЕР КУБОВ

### 🧱 Моделирование
1. Создай **Part** в Workspace и назови его `Spawner`.
2. Установи свойство `Anchored = true`.
3. Создай в Workspace папку **Folder** и назови её `SpawnedCubes`.

### 💻 Код (Script внутри Spawner)
```lua
-- Получаем доступ к самому спавнеру
local spawner = script.Parent

-- Ссылка на папку, куда будут складываться кубы
local folder = workspace.SpawnedCubes

-- Цикл создания 10 объектов
for i = 1, 10 do
	-- Создаём новый объект класса Part
	local cube = Instance.new("Part")

	-- Настраиваем размер куба
	cube.Size = Vector3.new(4, 4, 4)

	-- Устанавливаем позицию выше спавнера (каждый следующий выше на 5 единиц)
	cube.Position = spawner.Position + Vector3.new(0, i * 5, 0)

	-- Отключаем закрепление, чтобы куб подчинялся физике и падал
	cube.Anchored = false

	-- Помещаем созданный куб в заранее созданную папку
	cube.Parent = folder

	-- Делаем небольшую паузу между созданием кубов
	wait(0.5)
end

```
## 🔹 ЗАДАНИЕ 3. ЛАЗЕР, КОТОРЫЙ СТРЕЛЯЕТ САМ

### 🧱 Моделирование
1. Создай **Part** и назови его `Laser`.
2. Сделай его очень длинным по одной оси и тонким по остальным.
3. Обязательно включи `Anchored = true`.
4. Установи `Material = Neon` (так он будет светиться как настоящий лазер).

### 💻 Код (Script внутри Laser)
```lua
-- Получаем доступ к лазеру
local laser = script.Parent

-- Запоминаем исходный размер (чтобы знать, куда возвращаться)
local startSize = laser.Size

-- Запускаем бесконечный цикл стрельбы
while true do
	-- Эффект выстрела: резко увеличиваем размер по оси Z до 200 единиц
	laser.Size = Vector3.new(startSize.X, startSize.Y, 200)

	-- Ждём полсекунды, пока лазер "выстреливает"
	wait(0.5)

	-- Возвращаем лазер к исходному (короткому) размеру
	laser.Size = startSize

	-- Пауза перед следующим выстрелом
	wait(1)
end
```

🧠 Краткое объяснение
В этом задании мы не двигаем объект, а манипулируем его свойством Size. Поскольку лазер закреплён (Anchored), изменение размера выглядит как мгновенное выбрасывание луча. Использование переменной startSize позволяет коду всегда знать, до каких размеров нужно "схлопнуться" после выстрела.

## 🔹 ЗАДАНИЕ 4. СПАВНЕР С ОГРАНИЧЕНИЕМ

### 🧱 Моделирование
1. Создай папку **Folder** в Workspace и назови её `SpawnedObjects`.
2. Создай **Part** (сам спавнер) и назови его `AdvancedSpawner`.
3. Установи свойство `Anchored = true`.

### 💻 Код (Script внутри AdvancedSpawner)
```lua
-- Ссылка на спавнер
local spawner = script.Parent

-- Ссылка на папку, где лежат созданные объекты
local folder = workspace.SpawnedObjects

-- Настройки лимитов
local maxObjects = 5      -- Максимальное число кубов в игре
local spawnDelay = 1.5    -- Как часто пытаться создавать новый куб

-- Бесконечный цикл проверки
while true do
	-- Считаем количество объектов внутри папки через # и GetChildren()
	if #folder:GetChildren() < maxObjects then
		-- Если объектов меньше лимита — создаём новый
		local cube = Instance.new("Part")
		cube.Size = Vector3.new(4, 4, 4)
		cube.Position = spawner.Position + Vector3.new(0, 10, 0)
		cube.Anchored = false
		cube.Parent = folder

		-- Важный момент: удаляем этот куб автоматически через 6 секунд
		-- Debris не прерывает работу скрипта в ожидании удаления
		game:GetService("Debris"):AddItem(cube, 6)
	end

	-- Пауза перед следующей проверкой
	wait(spawnDelay)
end

```
🧠 Краткое объяснение
Этот спавнер работает "умнее". Прежде чем создать новый куб, он проверяет папку SpawnedObjects. Если там уже есть 5 кубов, он пропустит ход. Чтобы место в папке освобождалось, мы используем Debris:AddItem, который сам "прибирает" за собой старые объекты через заданное время.


## 🔹 ЗАДАНИЕ 5. ФИНАЛ — УМНЫЙ СТРАЖ

### 🧱 Моделирование
1. Создай **Part** и назови его `SmartGuard`.
2. Сделай его крупным, чтобы он выглядел угрожающе.
3. Установи свойство `Anchored = true`.
4. Можешь изменить цвет на красный для пущего эффекта.

### 💻 Код (Script внутри SmartGuard)
```lua
-- Настройки стража
local guard = script.Parent
local startPos = guard.Position
local patrolDistance = 25  -- Радиус патрулирования
local speed = 0.1          -- Обычная скорость
local alertSpeed = 0.25    -- Скорость при обнаружении игрока
local detectionRadius = 30 -- Дистанция "зрения"

local direction = 1        -- Направление движения (1 или -1)

-- Основной цикл искусственного интеллекта
while true do
	local players = game.Players:GetPlayers()
	local playerNear = false

	-- Проверяем каждого игрока на сервере
	for _, player in pairs(players) do
		-- Проверяем, есть ли у игрока персонаж и его главная часть (HumanoidRootPart)
		if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
			-- Считаем расстояние между стражем и игроком
			local distance = (player.Character.HumanoidRootPart.Position - guard.Position).Magnitude

			-- Если игрок вошел в зону обнаружения
			if distance < detectionRadius then
				playerNear = true
				break -- Дальше можно не искать, один игрок уже найден
			end
		end
	end

	-- Если игрок рядом — включаем режим погони (ускоряемся)
	local currentSpeed = playerNear and alertSpeed or speed

	-- Двигаем стража в текущем направлении
	guard.Position = guard.Position + Vector3.new(currentSpeed * direction, 0, 0)

	-- Если страж ушел слишком далеко от дома — разворачиваем его
	if (guard.Position - startPos).Magnitude >= patrolDistance then
		direction = direction * -1
	end

	-- Минимальная пауза для плавности работы
	wait(0.03)
end
```
🧠 Краткое объяснение
Это настоящий мини-ИИ. В каждом цикле скрипт «оглядывается» вокруг, перебирая всех игроков на сервере. С помощью математического свойства .Magnitude он вычисляет точное расстояние до цели. Если кто-то подошел слишком близко, переменная playerNear становится истинной (true), и страж переключается на повышенную скорость (alertSpeed).